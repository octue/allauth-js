// TODO Implement this hook. I don't use social providers, PRs accepted!
export const useProviders = () => {
  //     {config.data.socialaccount ? (
  // TODO IMPLEMENT PROVIDERS
  // ) : null}

  return { loading: false }
}
